# This file contains the fastlane.tools configuration
# You can find the documentation at https://docs.fastlane.tools
#
# For a list of all available actions, check out
#
#     https://docs.fastlane.tools/actions
#

# Uncomment the line if you want fastlane to automatically update itself
# update_fastlane

#####

# Project specific variables

workspace = "DevStack.xcworkspace"
slack_channel = "#ci"

alpha = {
  build_branch: "build",
  output_name: "A-DevStack",
  app_identifier: "cz.matee.devstack.alpha",
  scheme: "DevStack_Alpha",
  gsp_path: "./DevStack/App/Config/Alpha/GoogleService-Info.plist",
  testflight_groups: ['App Store Connect Users', 'Public'],
  testflight_link: "NOT AVAILABLE"
}

beta = {
  build_branch: "build",
  output_name: "B-DevStack",
  app_identifier: "cz.matee.devstack.beta",
  scheme: "DevStack_Beta",
  gsp_path: "./DevStack/App/Config/Beta/GoogleService-Info.plist",
  testflight_groups: ['App Store Connect Users', 'Public'],
  testflight_link: "NOT AVAILABLE"
}

production = {
  build_branch: "build",
  output_name: "DevStack",
  app_identifier: "cz.matee.devstack",
  scheme: "DevStack",
  gsp_path: "./DevStack/App/Config/Production/GoogleService-Info.plist",
  testflight_groups: ['App Store Connect Users', 'Public'],
  testflight_link: "NOT AVAILABLE"
}

#####

# Variables shared among all projects (do not change unless necessary)

matee = {
  apple_id: "mateefastlane@gmail.com",
  team_id_dev: "WNB34WBN42",
  team_id_connect: "120470944",
  match_branch: "matee",
  certificate: "iPhone Distribution: Matee Devs s.r.o."
}

match_url = "git@github.com:MateeDevs/match.git"
slack_url = "https://hooks.slack.com/services/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"

#####

# Variables to specify developer for production release

client = {
  apple_id: matee[:apple_id],
  team_id_dev: matee[:team_id_dev],
  team_id_connect: matee[:team_id_connect],
  match_branch: matee[:match_branch],
  certificate: matee[:certificate]
}

#####

default_platform(:ios)

platform :ios do
  before_all do
    ENV["SLACK_URL"] = slack_url
  end

  # SYNC SIGNING

  desc "Sync Alpha signing"
  lane :signing_alpha
    signing(alpha.merge(matee))
  end

  desc "Sync Beta signing"
  lane :signing_beta
    signing(beta.merge(matee))
  end

  desc "Sync Production signing"
  lane :signing_production
    signing(production.merge(matee))
  end

  desc "Sync signing"
  private_lane :signing do |config|
    sync_code_signing(
      username: config[:apple_id],
      team_id: config[:team_id_dev],
      app_identifier: config[:app_identifier],
      git_url: match_url,
      git_branch: config[:match_branch],
      type: "appstore"
    )
  end

  # BUILD

  desc "Create a new Alpha build"
  lane :build_alpha do |options|
    build(alpha.merge(matee).merge(options))
  end

  desc "Create a new Beta build"
  lane :build_beta do |options|
    build(beta.merge(matee).merge(options))
  end

  desc "Create a new Production build"
  lane :build_production do |options|
    build(production.merge(client).merge(options))
  end

  desc "Create a new build"
  private_lane :build do |config|

    unless config[:git_branch] =~ /#{config[:build_branch]}\/\d+\.\d+(\.\d+)?$/
      UI.user_error!("Invalid git branch: \"#{config[:git_branch]}\". Expect: \"#{config[:build_branch]}/x.x.x\"")
    end
    increment_version_number(version_number: config[:git_branch].split('/').last)

    unless config[:build_number] =~ /^\d{1,18}$/
      UI.user_error!("Invalid build number: \"#{config[:build_number]}\". Expect 1-18 digits")
    end
    increment_build_number(build_number: config[:build_number])

    keychain_name = "fastlane"
    keychain_password = SecureRandom.base64
    create_keychain(
      name: keychain_name,
      default_keychain: true,
      unlock: true,
      timeout: 3600,
      lock_when_sleeps: true,
      password: keychain_password
    )
    sync_code_signing(
      username: config[:apple_id],
      team_id: config[:team_id_dev],
      app_identifier: config[:app_identifier],
      git_url: match_url,
      git_branch: config[:match_branch],
      type: "appstore",
      keychain_name: keychain_name,
      keychain_password: keychain_password,
      readonly: true
    )
    build_ios_app(
      workspace: workspace,
      scheme: config[:scheme],
      clean: true,
      export_method: "app-store",
      codesigning_identity: config[:certificate],
      xcargs: "PROVISIONING_PROFILE_SPECIFIER='match AppStore #{config[:app_identifier]}'",
      archive_path: "#{config[:output_name]}.xcarchive",
      output_directory: "./ipa",
      output_name: config[:output_name]
    )
  end

  # UPLOAD TO THE TESTFLIGHT

  desc "Upload the Alpha build to the TestFlight"
  lane :upload_testflight_alpha do
    upload_testflight(alpha.merge(matee))
  end

  desc "Upload the Beta build to the TestFlight"
  lane :upload_testflight_beta do
    upload_testflight(beta.merge(matee))
  end

  desc "Upload the Production build to the TestFlight"
  lane :upload_testflight_production do
    upload_testflight(production.merge(client))
  end

  desc "Upload the build to the TestFlight"
  private_lane :upload_testflight do |config|
    version = get_ipa_info_plist_value(ipa: "./ipa/#{config[:output_name]}.ipa", key: "CFBundleShortVersionString")
    build = get_ipa_info_plist_value(ipa: "./ipa/#{config[:output_name]}.ipa", key: "CFBundleVersion")
    upload_to_testflight(
      ipa: "./ipa/#{config[:output_name]}.ipa",
      username: config[:apple_id],
      dev_portal_team_id: config[:team_id_dev],
      team_id: config[:team_id_connect],
      app_identifier: config[:app_identifier],
      skip_waiting_for_build_processing: true,
      # distribute_external: true,
      # groups: config[:testflight_groups],
      # changelog: "#{config[:output_name]} #{version} build #{build}"
    )
    slack(
      message: "[iOS #{config[:output_name]}] #{version} build #{build} uploaded to the TestFlight\nLink: #{config[:testflight_link]}",
      channel: slack_channel,
      default_payloads: [],
      payload: {}
    )
  end

  # UPLOAD TO THE APPSTORE

  desc "Upload the Production build to the AppStore"
  lane :upload_appstore_production do
    upload_appstore(production.merge(client))
  end

  desc "Upload the build to the AppStore"
  private_lane :upload_appstore do |config|
    version = get_info_plist_value(path: config[:plist_path], key: "CFBundleShortVersionString")
    build = get_info_plist_value(path: config[:plist_path], key: "CFBundleVersion")
    upload_to_app_store(
      ipa: "./ipa/#{config[:output_name]}.ipa",
      username: config[:apple_id],
      dev_portal_team_id: config[:team_id_dev],
      team_id: config[:team_id_connect],
      app_identifier: config[:app_identifier],
      force: true,
      skip_screenshots: true,
      skip_metadata: true
    )
    slack(
      message: "[iOS #{config[:output_name]}] #{version} build #{build} uploaded to the AppStore",
      channel: slack_channel,
      default_payloads: [],
      payload: {}
    )
  end

  # SYNC DSYM FILES

  desc "Sync Alpha dSYM files"
  lane :dsym_alpha
    dsym(alpha.merge(matee))
  end

  desc "Sync Beta dSYM files"
  lane :dsym_beta
    dsym(beta.merge(matee))
  end

  desc "Sync Production dSYM files"
  lane :dsym_production
    dsym(production.merge(matee))
  end

  desc "Sync dSYM files"
  private_lane :dsym do |config|
    download_dsyms(
      username: config[:apple_id],
      team_id: config[:team_id_connect],
      app_identifier: config[:app_identifier],
      version: 'latest'
    )
    upload_symbols_to_crashlytics(
      gsp_path: config[:gsp_path]
    )
    clean_build_artifacts
  end

  # You can define as many lanes as you want

  after_all do |lane|
    # This block is called, only if the executed lane was successful
  end

  error do |lane, exception|
    # This block is called, only if there was an error in the executed lane
  end
end
